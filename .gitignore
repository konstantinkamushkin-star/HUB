# Cursor IDE — логи могут быть сотни МБ, не коммитить
.cursor/

# Android — сборка и кэш Gradle (не коммитить)
DiveHubAndroid/app/build/
DiveHubAndroid/.gradle/
DiveHubAndroid/**/build/

# Локальные большие датасеты (при необходимости раскомментируйте)
# opendivemap_all_sites.geojson

# Локальные сравнения обработки фото — не для репозитория
tmp-photo-compare/

# npm (локальная установка для scripts/compare_nikolaj_bech_port.py)
backend/underwater-vision-module/node_modules/
backend/underwater-vision-module/package-lock.json
